<div class="columns is-centered">
  <div class="column is-half">
    <h2 class="title is-size-2"><%= @publisher.name %></h2>
    <p><strong>Number of Titles: </strong> <%= @publisher.title_count %></p>
      <table class="table is-fullwidth">
        <thead>
          <tr>
            <th>Name</th>
            <th>Link</th>
          </tr>
        </thead>
        <tbody>
          <% @publisher.video_game_titles.order("name ASC").each do |vgt|%>
            <tr>
              <td><%= vgt.name %></td>
              <td><%= link_to "Link", vgt %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <%= link_to "Back", video_game_titles_path %>
  </div>
</div>